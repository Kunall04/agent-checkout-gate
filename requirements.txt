# Pinned stack per the project spec §3.
fastapi==0.141.1
uvicorn==0.52.4
PyNaCl==1.6.2
PyYAML==6.0.3
razorpay==2.0.1
anthropic==1.3.0
python-dotenv==1.2.3

# Beyond §3: §6 requires JSON Schema validation of untrusted model output, and
# a hand-rolled validator is the wrong place to save fifty lines.
jsonschema==4.26.0

# Test only.
pytest==9.1.1
httpx==0.28.1
